[phases.setup]
nixPkgs = ['nodejs', 'yarn']

[[services]]
name = "frontend"
source = "client/"
build = "npm install && npm run build"
start = "npx serve -s build"
targetPort = 3000

[[services]]
name = "backend"
source = "server/"
build = "npm install"
start = "npm start"
targetPort = 5000
